# 关联文档

| 类型 | 文档路径 |
|:---|:---|
| 原始需求 | `kinit/doc/01-or/【登录认证】账号与手机号登录及管理员初始化.md` |
| 开发设计 | `kinit/doc/02-dr/02 登录认证与初始化.md` |

# 测试范围

- 登录标识分类：国内手机号 / 含字母账号 / 非法输入（11 位非法号段、无字母非手机等）。
- 初始化数据：明文密码行插入前被哈希；已是 `$2b$...` 的行不二次哈希。

# 测试用例

| 编号 | 前置条件 | 操作步骤 | 预期结果 | 自动化 |
|:---|:---|:---|:---|:---|
| TC-LI-01 | 无 | 对合法手机号字符串调用分类/校验函数 | 识别为手机号类型 | pytest：`core/login_identifier` |
| TC-LI-02 | 无 | 对含字母的字符串（如 `admin`） | 识别为 name/账号类型 | pytest |
| TC-LI-03 | 无 | 对 11 位全数字但不符合号段 | 抛出/校验失败「请输入正确手机号」 | pytest |
| TC-LI-04 | 无 | 对无字母且非合法手机号的短字符串 | 抛出「账号须包含字母」 | pytest |
| TC-INIT-01 | 无 | 明文密码 `secret` 经初始化预处理 | 得到 bcrypt 前缀哈希 | pytest：`prepare_vadmin_auth_user_rows` |
| TC-INIT-02 | 无 | 密码列为整型或 `密码` 表头 | 同样得到 bcrypt，且输出行仅含键 `password` | pytest |

# 自动化测试标识

- 框架：**pytest**，目录：`kinit/kinit-api/test/test_core/test_login_identifier.py`（登录标识规则与 `prepare_vadmin_auth_user_rows`）。
- **E2E**：当前 kinit-admin 工程未接入 Playwright；前端变更以手工回归为主，后续若补齐 `test/e2e` 可补充登录页展示与提交流程用例。
