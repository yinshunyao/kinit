# `kinit/doc/02-dr/` 目录说明

## 目录用途

存放 kinit 的**开发设计**文档，说明模块行为、数据模型、接口与边界。

## 文档范围

- 允许：功能设计、数据模型、API、约束与关联文档表。
- 禁止：替代 OR 写需求原文；测试步骤细节放在 `../03-tr/`。
- 当前文档示例：`01 MongoDB关闭时MySQL适配.md`、`02 登录认证与初始化.md`。

## 编辑规则

- 每个设计文档须含 `# 关联文档`，至少关联对应 `01-or` 文档。
- 变更设计时同步更新 `../03-tr/` 中对应测试设计。
